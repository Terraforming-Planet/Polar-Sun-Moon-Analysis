# Ocean Research Station public source

The production Ocean Research Station is maintained in `docs/ocean-station/`. This marker documents that the page is an intentional persistent GitHub Pages module. `force-pages-deploy.yml` does not delete custom `docs/ocean-station/` content while rebuilding Vite assets.
