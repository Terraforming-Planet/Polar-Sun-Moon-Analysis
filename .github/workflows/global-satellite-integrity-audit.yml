name: Global satellite integrity audit

on:
  pull_request:
    branches: ["main"]
    paths:
      - 'terra_integrity/**'
      - 'config/satellite_integrity_sources.json'
      - 'tests/test_global_satellite_integrity.py'
      - '.github/workflows/global-satellite-integrity-audit.yml'
  push:
    branches: ["main"]
    paths:
      - 'terra_integrity/**'
      - 'config/satellite_integrity_sources.json'
      - '.github/workflows/global-satellite-integrity-audit.yml'
  workflow_dispatch:
  schedule:
    - cron: '17 5 * * *'

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - name: Checkout audit code
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          filter: blob:none

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install audit runtime
        run: |
          python -m pip install --upgrade pip
          python -m pip install Pillow

      - name: Run Tests 001-015 cross-test SHA/source-product audit
        run: |
          python -m terra_integrity.manifest_audit --no-fail

      - name: Upload integrity report
        uses: actions/upload-artifact@v4
        with:
          name: GLOBAL_DATA_INTEGRITY_REPORT
          path: |
            docs/data/integrity/GLOBAL_DATA_INTEGRITY_REPORT.json
            docs/data/integrity/GLOBAL_DATA_INTEGRITY_REPORT.md
          retention-days: 90

      - name: Enforce integrity gate
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          report = json.loads(
              Path('docs/data/integrity/GLOBAL_DATA_INTEGRITY_REPORT.json').read_text()
          )
          print('Status:', report['status'])
          print('Summary:', report['summary'])
          print('SHA coverage:', report['exact_hash_coverage'], '/', report['accepted_count'])
          print(
              'Perceptual-hash coverage:',
              report['perceptual_hash_coverage'],
              '/',
              report['accepted_count'],
          )
          if report['exact_hash_coverage'] != report['accepted_count']:
              raise SystemExit('Global audit does not have complete SHA-256 coverage')
          if report['status'] != 'PASS':
              raise SystemExit('Global satellite integrity audit failed')
          PY
