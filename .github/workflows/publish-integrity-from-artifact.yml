name: Fast publish Test 014 integrity from validated artifact

on:
  push:
    branches: [experiment-014-wisla-gniew-grudziadz-53-660000-18-790000]
    paths: ['.github/workflows/publish-integrity-from-artifact.yml']
  workflow_dispatch:

permissions:
  contents: write
  actions: read

concurrency:
  group: fast-publish-test014-integrity
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    env:
      SOURCE_RUN_ID: '31920029537'
      SOURCE_ARTIFACT: TEST014_WISLA_GNIEW_GRUDZIADZ_1990_2026_SPRING_AUTUMN
      EXPERIMENT_DIR: experiments/experiment_014_wisla_gniew_grudziadz
    steps:
      - uses: actions/checkout@v4
        with:
          ref: experiment-014-wisla-gniew-grudziadz-53-660000-18-790000
          fetch-depth: 1
          filter: blob:none
          sparse-checkout: |
            tools
            published
            .github
          sparse-checkout-cone-mode: true
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python -m pip install Pillow
      - name: Download validated artifact
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          rm -rf /tmp/test014 && mkdir -p /tmp/test014
          gh run download "$SOURCE_RUN_ID" --repo "$GITHUB_REPOSITORY" --name "$SOURCE_ARTIFACT" --dir /tmp/test014
      - name: Restore active seasonal evidence from combined ZIP
        run: |
          python - <<'PY'
          import os, shutil, zipfile
          from pathlib import Path
          src=Path('/tmp/test014')
          candidates=[p for p in src.rglob('*.zip') if 'SPRING_AUTUMN' in p.name.upper()]
          if not candidates: raise SystemExit('combined seasonal ZIP not found')
          combined=max(candidates,key=lambda p:p.stat().st_size)
          dest=Path(os.environ['EXPERIMENT_DIR'])/'seasonal_evidence'
          shutil.rmtree(dest,ignore_errors=True); dest.mkdir(parents=True,exist_ok=True)
          with zipfile.ZipFile(combined) as zf:
              members=[n for n in zf.namelist() if n.startswith(('spring/','autumn/'))]
              if not members: raise SystemExit('spring/autumn members not found')
              zf.extractall(dest,members)
          for season in ('spring','autumn'):
              assert (dest/season/'manifest.json').exists()
              assert any((dest/season/'images').iterdir())
          print('restored',combined)
          PY
      - name: Publish SHA-256, perceptual hashes and gallery
        run: python tools/publish_experiment_integrity.py --test 14 --experiment-dir "$EXPERIMENT_DIR" --gallery
      - name: Commit lightweight publication
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add --sparse -f published/experiment-014
          if git diff --cached --quiet; then exit 0; fi
          git commit -m 'data(test014): publish validated integrity index and gallery'
          git push origin HEAD:experiment-014-wisla-gniew-grudziadz-53-660000-18-790000
