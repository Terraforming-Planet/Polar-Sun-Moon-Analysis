# Polar Sun Moon Analysis

Production-quality Python 3.12 scientific project for analyzing seasonal apparent altitude of the Sun and Moon at the North and South Poles during vernal and autumnal equinoxes from 2006 through 2024.

## Scientific scope

This repository uses **only official NASA JPL Horizons ephemerides** through the official Horizons API:

<https://ssd.jpl.nasa.gov/api/horizons.api>

The project does not use machine learning, image processing, Prithvi, lunar phases, or monthly orbital analysis. It downloads Horizons observer ephemerides, validates required quantities, stores observations in Pandas DataFrames, computes statistics, and exports CSV, Excel, figures, and a PDF report.

## Computed quantities

For every generated vernal and autumnal equinox from 2006 through 2024, the pipeline computes the following for the North Pole (90°N) and South Pole (90°S):

- Sun apparent altitude above the horizon
- Moon apparent altitude above the horizon
- Sun declination
- Moon declination

Equinox instants are generated by finding the apparent geocentric Sun declination zero crossing using NASA JPL Horizons responses. Required ephemeris fields are validated before processing. If Horizons omits a required quantity, the program raises an error containing the exact NASA Horizons response or parsed row instead of inventing values.

## Outputs

Generated files are written under `outputs/` by default:

- `observations.csv`
- `statistics.csv`
- `polar_equinox_analysis.xlsx`
- `scientific_summary.txt`
- `polar_equinox_report.pdf`
- `figures/apparent_altitude_deg.png`
- `figures/declination_deg.png`

## Installation

```bash
python -m venv .venv
source .venv/bin/activate  # Windows: .venv\\Scripts\\activate
python -m pip install --upgrade pip
python -m pip install -r requirements.txt
```

Google Colab users can upload the repository and run:

```python
!pip install -r requirements.txt
!python -m polar_equinox_analysis
```

## Usage

Run the full 2006-2024 analysis:

```bash
python -m polar_equinox_analysis
```

Customize output, cache, or years:

```bash
python -m polar_equinox_analysis --start-year 2006 --end-year 2024 --output-dir outputs --cache-dir .cache/horizons
```

## Architecture

- `polar_equinox_analysis.models`: typed dataclasses for observers, equinoxes, and observations.
- `polar_equinox_analysis.horizons`: cached, retrying official Horizons API client and response validation.
- `polar_equinox_analysis.equinox`: equinox generation from Horizons Sun declination zero crossings.
- `polar_equinox_analysis.analysis`: Pandas/Numpy statistical summaries.
- `polar_equinox_analysis.reporting`: CSV, Excel, Matplotlib figures, and PDF report export.
- `polar_equinox_analysis.pipeline`: object-oriented orchestration.
- `polar_equinox_analysis.__main__`: command-line interface.

## Statistical analysis

For each pole, season, body, and quantity the project computes:

- mean
- standard deviation
- minimum
- maximum
- linear trend
- second-degree polynomial trend
- correlation with year

The scientific summary is generated only from computed Horizons ephemerides and statistics.

## Reproducibility and safeguards

- Official NASA JPL Horizons API only.
- Automatic retry with exponential backoff for API failures.
- File-based caching of raw Horizons responses.
- Logging for network, cache, and pipeline progress.
- No fabricated astronomical values.
- No non-Horizons astronomical data sources.
- PEP8-oriented modular Python with type hints and documentation.

## License

MIT License. See `LICENSE`.
